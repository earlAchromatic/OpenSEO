#!/usr/bin/env node

import 'zx/globals';

import { program } from 'commander';

//initialize directory called OpenSEO
await $`mkdir openSEO`;
//npm install the client/server packages

// run build command for those packages - why build? Why not just download the prebuilt packages? Skip this step for now.

// once built, launch koa server & launch http-server with client

// test script to test communications

// run initial audit on specified dist file
// if no dist specified, request that in the command line input
